# FrameFlow — To'liq launch-tayyorlik auditi (2026-07-02)

Metod: 11 yo'nalishli parallel auditor (real kod o'qildi) → 24 kritik topilma adversarial tekshirildi → 5-fazali reja.
Natija: **90 topilma** — P0:2, P1:15, P2:21, P3:32, P4:20. Tasdiqlangan kritik: 20/24 (1 noto'g'ri-pozitiv, 3 severity pasaytirildi).

## Umumiy baho: READY-WITH-FIXES

Zanjir (Contributor Studio → Admin approve → AE Plugin + Web platforma) **kod darajasida to'liq ulangan**, lekin
productionda haqiqiy launch uchun **tayyor emas** — bir nechta ketma-ket bloker bor. Ko'p defekt hozir "latent"
(katalog bo'sh + Paddle yo'q bo'lgani uchun aktiv buzilmagan), lekin birinchi shablon publish + birinchi migratsiya +
to'lov yoqilishi bilan portlaydi.

## INTEGRATSIYA JAVOBI (Studio → Plugin + Platforma bir xilmi?)

**HA, o'qish/ko'rsatish oqimi to'g'ri ulangan**, lekin YOZISH va URL tomonida 2 bloker bor:

Ishlaydigan qismlar:
- Studio thumb/preview'ni presigned PUT bilan to'g'ridan GCS'ga yuklaydi.
- Admin approve → `reviewStatus=APPROVED + published=true`; katalog filtri to'g'ri (`catalog-map.ts` approvedCatalogWhere).
- `GET /api/plugin/catalog` HAM AE plugin, HAM web platforma'ga bir xil `mapCatalogItem` javobini beradi.
- Kredit — plugin (`/api/plugin/ai`) va web (`/api/studio`) BIR XIL kredit hovuzini (plugin-profile) ishlatadi; `requireAuth` ham plugin token, ham studio JWT qabul qiladi.

Uzilishlar:
- **UZILISH #1 (P0):** Pack (.aep/.zip) presigned EMAS — `uploadUrlSchema` faqat thumb/preview qabul qiladi (contributor.ts:836), pack esa multer POST /assets orqali app so'rov tanasidan oqadi. Cloud Run 32MB limiti tufayli 100MB–GB AE pack 413 bilan rad etiladi → **Contributor real shablon yuklolmaydi → zanjir amalda ishlamaydi**.
- **UZILISH #2 (P1):** `CDN_BASE_URL` bo'sh → `getPublicUrl()` (s3.ts:46) o'lik AWS host (`<bucket>.s3.auto.amazonaws.com`) qaytaradi → birinchi published shablonda thumb/preview HAM web, HAM plugin'da buziladi.
- **Web divergensiya:** platforma author'ni `it.contributor.name` (mavjud emas) o'qib doim "FrameFlow" ko'rsatadi; count>1 rasm gen'da faqat 1-asset ko'rsatiladi (to'langan 2-4 rasm web'da yo'qoladi — plugin to'g'ri).

## 5 ENG KATTA XAVF

| # | Xavf | Sev | Fayl |
|---|------|-----|------|
| 1 | Pack presigned emas — real shablon yuklanmaydi (32MB limit) | P0 | contributor.ts:836 |
| 2 | CDN_BASE_URL bo'sh — barcha thumb/preview buzuq | P1 | s3.ts:46, cloudrun-env.yaml |
| 3 | Deploy'da `migrate deploy` gate yo'q — yangi migratsiya jimgina qo'llanmay crash | P1 | deploy-cloudrun.yml:73 |
| 4 | POST /api/logs escape'siz → admin panelida saqlangan XSS (imtiyoz eskalatsiyasi) | P1 | logs.ts:60, admin-logs.js:100 |
| 5 | Email verify + byudjet cap yo'q — tekin-kredit bot abuzasi | P1 | auth.ts:44, GCP Billing |
| + | Forgot-password havolasi /studio prefiksi tufayli 404 | P1 | auth.ts:168 |

---

## 5-FAZALI TUZATISH REJASI

### Faza 1 — Xavfsizlik + Launch-blokerlar (MAJBURIY, ~3-5 kun)
Maqsad: zanjir end-to-end ishlaydi, admin XSS yopiladi, forgot-password tuzaladi, abuse to'siladi.
1. **[P0]** Pack uchun presigned PUT: `uploadUrlSchema` enum'iga "pack", studio-api.js direct filtriga pack, yangi `POST /templates/:id/pack-uploaded` → `downloadS3ToFile`+`storeMogrtScenesFromZip` fon'da. *(contributor.ts:836, studio-api.js:219, contributor.ts:495-511)*
2. **[P1]** `CDN_BASE_URL=https://storage.googleapis.com/assetflow-assets-2026` + getPublicUrl'ni path-style GCS URL qiladigan tuzatish; bucket public-read tekshirish. *(s3.ts:46, cloudrun-env.yaml)*
3. **[P1]** deploy-cloudrun.yml'ga deploy'dan OLDIN gated `migrate:deploy` qadami; render.yaml'ni .deprecated. *(deploy-cloudrun.yml:73)*
4. **[P1]** Admin XSS: admin-logs.js:100 `escapeHtml(...)`; logs.ts POST'ni allowlist + rol gate. *(admin-logs.js:100, logs.ts:60)*
5. **[P1]** Forgot-password: auth.ts:168 resetUrl'dan /studio prefiksini olib tashlash; login.html href absolyut. *(auth.ts:168, login.html)*
6. **[P1]** Abuse: GCP Billing budget+alert & global spend cap; register'ga Turnstile captcha; emailVerified→consumeAiCredits gate. *(auth.ts:44, plugin-profile.ts:319)*

### Faza 2 — Integratsiya + Pul-yo'qotish (~3-4 kun)
1. **[P1]** count>1 gen: mapGen'ni BARCHA assets surfacing; filmstrip/lightbox grid; download asset indeksi. *(index.html:15644, 15814)*
2. **[P2]** Web katalog author: `author: it.author || 'FrameFlow'`. *(index.html:15606)*
3. **[P2]** refundAiCredits oy-chegarasi leak: Generation'ga refunded flag; refund cap `LEAST(aiCredits+cost, allotment)`. *(plugin-profile.ts:361)*
4. **[P2]** Admin obunachi amallarini audit'ga yozish (block/plan/kredit/limit). *(admin.ts:209)*
5. **[P2]** Server-tomon PRO gate: guardDownloadable'ga isPro → 402 PRO_REQUIRED (Paddle'dan OLDIN). *(plugin.ts:186)*
6. **[P2]** CreditLedger + ProviderSpend modellari (moliyaviy audit izi). *(plugin-profile.ts:345, studio-gen.ts:142)*

### Faza 3 — Masshtab + Ishonchlilik (~4-6 kun)
1. **[P2]** Cloud Run `--concurrency 20-40` + Neon POOLED endpoint (pgbouncer=true) + alohida directUrl. *(cloudrun-env.yaml, database/src/index.ts)*
2. **[P2]** SSRF himoyasi: markaziy fetchSafe (faqat data-URI + bizning GCS; private IP bloklash; redirect:error). *(vertex-enhance.ts:86, gen-processor.ts:547/635)*
3. **[P2]** Error tracking (GCP Error Reporting/Sentry) + readiness health (SELECT 1) + alert. *(index.ts:114, 73)*
4. **[P2]** admin/plugin-subscribers N+1 tuzatish; genAsset sizeBytes saqlab HeadObject'larni yo'q qilish. *(admin.ts:99, studio-gen.ts:67)*
5. **[P2]** Deploy xavfsizligi: --no-traffic --tag candidate + smoke gate + rollback; Secret Manager. *(deploy-cloudrun.yml:76)*
6. **[P3]** pollJob backstop: max davomiylik + xato sanoq → UI qulflanmaydi. *(index.html:15772)*

### Faza 4 — UX + Halollik (~3-4 kun)
1. **[P2]** Landing halollik: soxta statistika (10K/5M/12M) va soxta testimoniallar (Dilnoza K...) olib tashlash/real qilish. *(index.html:15983, 15439)*
2. **[P2]** Huquqiy havolalar tirik: footer → /privacy.html /terms.html /refund.html; o'zbekchaga tarjima (Paddle SHART). *(index.html:14808)*
3. **[P2]** Katalog cold-start skeleton; loadModels xato'da qayta urinish. *(index.html:14988, 15926)*
4. **[P2]** Admin Sozlamalar real backend'ga ulash; soxta CSV/xabar/bloklash-sabab tugmalarini real qilish yoki olib tashlash. *(admin-views2.js:373, admin-subscribers.js:479)*
5. **[P3]** Mobil UX: app header (<680px) wrap; marketplace filtr bottom-sheet. *(index.html:14862)*

### Faza 5 — Polish + Rebrand + To'lov (Faza F/G, ~5-8 kun)
1. **[P2]** Plugin rebrand (Faza G): manifest.xml com.frameflow.panel; 7 faylda PROD_API=api.getframeflow.app; build-zxp.sh css/ stage'ga; imzolangan .zxp. *(manifest.xml:4, assetflow-env.js:5)*
2. **[P2]** Download event tracking (Faza C): TemplateDownloadEvent modeli — jonli lenta poydevori. *(plugin.ts:487)*
3. **[P3]** Domen izchilligi: admin/contributor meta-tag → api.getframeflow.app; keepalive eski onrender olib tashlash. *(admin/index.html:6)*
4. **[P3]** Paddle (Faza F); gen download Content-Disposition:attachment. *(index.html:16064)*
5. **[P3]** Redis rate-limit + email DKIM/SPF; migration_lock.toml; voice model Google TTS yoki "tez orada". *(rate-limit.ts:24, email.ts:11)*

---

## To'liq topilmalar
Xom natija (barcha 90 topilma + adversarial verdictlar): `scratchpad`dagi workflow output (wd6oy9jmc).
byArea: UX:18, Infra:14, AI-Tools:9, Scale:9, Admin:9, Data:8, Security:7, Plugin:5, Platform:4, Integration:4, API:3.
