# SESSION-REPORT — Windows CI job'i (haqiqiy MSI tekshiruvi, 2026-07-22)

**1 commit, push/deploy/imzolash YO'Q. Diff: `.github/workflows/ci.yml` + 2 yangi plagin skripti (`ci-verify-win-install.ps1`, `test-ci-windows-installer.mjs`) + `installer-wix.mjs` (kichik eksport/CLI) + `package.json` (1 skript) + 3 hujjat. Backend/DB/auth/billing/pul-zona/panel runtime/payload fayllari TEGILMAGAN.**

- **Yopilgan bo'shliq:** 1cc01ad migratsiyasi (`RemoveFile`) faqat *generatsiya* darajasida isbotlangan edi — haqiqiy `wix build`, ICE validatsiyasi va eski o'rnatma ustidan o'rnatish hech qachon bajarilmagan (macOS'da toolchain yo'q).
- **Yangi `windows-installer` job'i** (har PR + `main` push, `windows-latest`, 30 daq, `contents: read`): qadalgan WiX (`--version 5.0.2`, `latest`/`--prerelease` YO'Q, versiya solishtiriladi) → **haqiqiy** `build-installer-win.mjs --unsigned` → **`wix msi validate`** (hech qanday ICE bostirilmaydi; `-sval`/`-sw` na workflow'da, na build skriptida) → `ci-verify-win-install.ps1`.
- **PowerShell isboti:** AYNAN `obsoleteInstallFiles()` 4 qoldig'i + alohida `assetflow-data` sentinel ekiladi → `msiexec /i /qn` (exit AYNAN 0) → o'rnatilgan ro'yxat manba payload'iga aynan teng + har fayl **SHA-256 bayt-ba-bayt** → 4 qoldiq O'CHGAN, sentinel SAQLANGAN, HKCU migratsiya keypath'i bor, `Program Files` toza → `msiexec /x /qn` (exit 0) → payload ketdi, sentinel YANA saqlandi.
- **Chegaralar:** ro'yxatlar CI'da takrorlanmaydi (hammasi mavjud CLI'lardan: `payload-files`/`stale-files`/`stage`/`field`/yangi `cleanup-registry`) · `GITHUB_ACTIONS=true` bo'lmasa skript ishlamaydi · nishon yo'l qattiq tekshiriladi, mavjud o'rnatma ustida RAD ETILADI · `finally`da FAQAT o'sha per-user papka o'chadi (joker/profil ildizi/Program Files/registr YO'Q) · sir/sertifikat/`signtool` YO'Q · **imzolanmagan `.msi` artefakt sifatida yuklanmaydi**.
- **Yangi test:** `npm run test:ci-windows-installer` — **112/112**, jonli `ci.yml`/`.ps1`/build skriptini o'qiydi va CLI'larni haqiqatan bajaradi. **6 mutatsiya bilan isbotlandi** (WiX pini olib tashlash · validator yo'q · `upload-artifact` · job yo'qolishi · keng `Remove-Item` · ro'yxat nusxasi) — har biri yiqiladi.
- **Regressiya YO'Q:** `test:plugin-installers` **229/229** · `test:plugin-package` **47/47** · `test:plugin-updater` **118/118** · `test:release-contract` **108/108** · `test:plugin-download-state` **10/10** · `npm run build -w apps/api` OK. Generatsiya qilingan `.wxs` HEAD bilan **bayt-ba-bayt bir xil** (22068 b) — `CLEANUP_REGISTRY_KEY` eksporti chiqishni o'zgartirmadi.
- **Lokal tekshiruv:** `node --check` (2 mjs) · JSON parse · 3 workflow YAML parse (js-yaml) · `.ps1` qavs/qo'shtirnoq balansi + apostrof skani. **Bajarib BO'LMADI (halol):** macOS'da `pwsh`, `msiexec`, `wix`, Windows registri YO'Q.
- **QOLGAN CHEKLOV:** bu job **hali masofada ishlamagan** — push qilinib **birinchi yashil run** bo'lmaguncha `wix build` / ICE / haqiqiy o'rnatish natijasi TASDIQLANMAGAN. Imzolash kredensiallari (Apple Developer ID + notarizatsiya, Windows Authenticode/EV) hamon ega ishi.
