# SESSION-REPORT — Windows CI: PowerShell skalyar-unroll tuzatmasi (2026-07-22)

**1 commit, push/deploy/imzolash YO'Q. Diff: `ci-verify-win-install.ps1` (+19/−4) + `test-ci-windows-installer.mjs` (+90) + `RELEASE-ARCHITECTURE.md`. Generator, MSI avtorlash, ICE64 yechimi, imzolash chegaralari, backend/DB/auth/billing/pul-zona — TEGILMAGAN.**

- **MASOFAVIY DALIL:** `a99edb7` → run **29901585416**, job **88863312477**. **ICE64 MASOFADA YOPILDI** — `wix msi validate` faqat `warning WIX1076: ICE91` bilan davom etdi va 5-qadamga (`ci-verify-win-install.ps1`) YETDI. Yangi yiqilish: **111-qator** → `cleanup-registry kontrakti kutilmagan shaklda: S`.
- **ILDIZ SABAB (MSI nuqsoni EMAS):** PowerShell funksiya natijasini pipeline'ga **unroll** qiladi — bir elementli massiv chaqiruvchida oddiy `[string]` bo'ladi. Shuning uchun `(Invoke-NodeLines …)[0]` qatorni emas, **birinchi HARFNI** qaytardi: `Software\FrameFlow\Plugin\Migration<TAB>obsolete-internal-files` → `"S"` → tab bo'yicha 2 maydon chiqmadi → fail-closed throw. Lokal CLI chiqishi TO'G'RI edi (1 qator, 1 tab) — xato faqat o'qigichда.
- **TUZATISH:** yangi `Invoke-NodeLine` (birlik) o'qigichi — `@()` bilan qayta massivga o'raydi, **0 yoki 2+ qatorda throw** (fail-closed), TO'LIQ qatorni qaytaradi. HAR IKKALA xavfli iste'molchi ko'chirildi: `$installDirName` (u ham `"c"` bo'lib keyingi qadamda yiqilardi) va `$cleanupReg`. Ko'p qatorlik iste'molchilar (`stale-files`, `payload-files`, `stage`) `Invoke-NodeLines`da SAQLANDI (+ `@()` bilan aniq massiv).
- **YANGI TESTLAR (+18):** `test:ci-windows-installer` **135/135** (edi 117). Jonli ps1 manbasi VA haqiqiy CLI chiqishi tekshiriladi: har ikkala single-line kontrakt aynan 1 qator berishi · ps1'ning O'Z `installDirName` regex'i manbadan o'qilib qo'llanishi · xavfli `(…)[0]` naqshi YO'Qligi · `Invoke-NodeLine` fail-closed ekani · ko'p qatorlik iste'molchilar saqlangani.
- **MUTATSIYA ISBOTI:** ps1 HEAD (buzuq) holatiga vaqtincha qaytarildi → yangi tekshiruvlardan **9 tasi YIQILDI** (126 passed / 9 failed), jumladan ikkala single-line kontrakt ham; fayl tiklandi. Test PS unroll semantikasini haqiqiy CLI chiqishida modellaydi: eski naqsh `"S"` (2 maydon emas) va `"c"` (ps1 regex'idan o'tmaydi).
- **REGRESSIYA YO'Q:** `test:plugin-installers` 244/244 · `test:plugin-package` 47/47 · `test:plugin-updater` 118/118 · `test:release-contract` 108/108 · `test:plugin-download-state` 10/10 · `node --check` OK · `ci.yml` YAML parse OK (jobs: build, windows-installer). API build tegishli emas (apps/api diffда yo'q).
- **QOLGAN CHEKLOV (halol):** macOS'da **`pwsh` YO'Q** (`which pwsh` → not found) → ps1 lokal BAJARILMADI; tuzatma statik + CLI-kontrakt darajasida isbotlandi. **Haqiqiy `msiexec` HALI BIR MARTA HAM ISHLAMAGAN** — o'rnatish/migratsiya/o'chirish isboti keyingi push va birinchi yashil run bilan tasdiqlanadi. Imzolash kredensiallari hamon ega ishi.
