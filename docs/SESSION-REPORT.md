# SESSION-REPORT — Windows MSI ICE64 tuzatmasi (2026-07-22)

**1 commit, push/deploy/imzolash YO'Q. Diff: `installer-wix.mjs` (generator) + `ci-verify-win-install.ps1` (+9 qator isbot) + 2 test + 3 hujjat. Backend/DB/auth/billing/pul-zona/panel runtime/payload fayllari TEGILMAGAN.**

- **MASOFAVIY DALIL (taxmin emas):** `d1e44e8` push qilindi → run **29878659236**: Linux build · CF Pages · deploy YASHIL, **`windows-installer` QIZIL**. WiX 5.0.2 o'rnatildi, haqiqiy imzosiz MSI MUVAFFAQIYATLI qurildi, lekin `wix msi validate` **10 marta** `error WIX0204: ICE64: The directory <X> is in the user profile but is not listed in the RemoveFile table` bilan yiqildi (`wix.exe` exit **204**) → **haqiqiy o'rnatish/migratsiya/o'chirish qadami umuman ishlamadi**.
- **ILDIZ SABAB (rasmiy semantika):** `AppDataFolder` ostida e'lon qilingan HAR BIR `Directory` qatori `RemoveFile` jadvalida bo'lishi SHART (FileName NULL = `<RemoveFolder>`). Generator 9 ta papka (`FF_Adobe`/`FF_CEP`/`FF_extensions`/`INSTALLFOLDER` + 5 payload papkasi) e'lon qilib, birortasiga ham bermas edi; ustiga `RemoveFile @Subdirectory="CSXS"` WiX'da 10-chi, **avto-Id'li** papka qatorini yasardi — unga `RemoveFolder` biriktirib bo'lmaydi. 9+1 = aynan 10 xato.
- **TUZATISH (bostirish EMAS):** (1) eski-fayl qatorlari endi `Subdirectory=` o'rniga daraxtdagi haqiqiy `Directory="D_CSXS_…"` Id'siga ishora qiladi → avto-papka qatori umuman yo'q; (2) yangi DOIMIY per-user komponent `FF_ProfileFolders` (HKCU keypath) har papka uchun AYNAN bitta `<RemoveFolder On="uninstall"/>` beradi, chuqurdan yuzaga. MSI bunday qatorni **FAQAT BO'SH papkaga** qo'llaydi → `assetflow-data` bor nishon papka ham, umumiy `…\Adobe\CEP\extensions` ham SAQLANADI.
- **36 ta `warning WIX1076: ICE91`** — ogohlantirish, chiqish kodiga ta'sir qilmaydi; ataylab per-user paket uchun MUQARRAR (`AppDataFolder` `ALLUSERS`ga qarab o'zgarmaydi) → **TEGILMADI, bostirilmadi**. `-sval`/`-sw`/`-sice`/`SuppressValidation`/`continue-on-error`/try-catch — hech biri qo'shilmadi (testlar buni majburlaydi).
- **Per-user shartnoma o'zgarmadi:** `Scope="perUser"`, `%APPDATA%\Adobe\CEP\extensions\com.frameflow`, UAC/admin/Program Files/CustomAction/`RemoveFolderEx`/sertifikat YO'Q; payload va eski-fayl ro'yxati bayt-bir-xil (yagona manba `package-flavors.mjs` / `obsoleteInstallFiles()`).
- **Yangi testlar:** `test:plugin-installers` **244/244** (edi 229 — +15: ICE64 qamrovi, tartib, `Subdirectory` taqiqi, papka komponenti, per-user/elevation doirasi) · `test:ci-windows-installer` **117/117** (edi 112 — +5: ICE bostirish/`continue-on-error`/try-catch/exit yumshatish taqiqi + yangi ps1 isboti).
- **MUTATSIYA ISBOTI:** `d1e44e8` generatori vaqtincha yuklab ko'rildi → yangi 5 ta asosiy tekshiruv **5/5 YIQILDI** (eski `.wxs`: Directory=9, RemoveFolder=0, Subdirectory=1 — masofaviy logdagi 10 xato bilan aynan mos). `ci.yml`ga 2 mutatsiya (`continue-on-error`, `-sice:ICE64`) → har biri yiqildi, fayl HEAD holatiga tiklandi.
- **Regressiya YO'Q:** `test:plugin-package` 47/47 · `test:plugin-updater` 118/118 · `test:release-contract` 108/108 · `test:plugin-download-state` 10/10 · `npm run build -w apps/api` OK · `node --check` (5 mjs) · 3 workflow YAML parse · generatsiya qilingan `.wxs` `xmllint` toza · `git diff --check` toza.
- **QOLGAN CHEKLOV (halol):** macOS'da `wix`/`msiexec`/`pwsh` YO'Q → tuzatma **masofada ISBOTLANMAGAN**. `wix msi validate` toza o'tishi va haqiqiy o'rnatish/migratsiya/o'chirish faqat **keyingi push va birinchi yashil run** bilan tasdiqlanadi. Imzolash kredensiallari (Apple Developer ID + notarizatsiya · Windows Authenticode) hamon ega ishi.
