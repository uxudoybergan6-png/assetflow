# SESSION-REPORT — Task 3: installer artefakt quvuri (2026-07-22)

**1 commit, push/deploy YO'Q, hech narsa o'rnatilmadi (`~/Library`ga tegilmadi). Pul/kredit/billing/auth/AI/katalog/payout diff'da YO'Q. Task 2 updater bloki, `/api/plugin/version`, `plugin-release-contract.ts` va admin publish qoidalari TEGILMAGAN.**

- **Yangi fayllar:** `scripts/installer-payload.mjs` (yagona payload manbai — `package-flavors.mjs` mijoz ro'yxati; `admin` flavor KOD DARAJASIDA rad etiladi; Admin sirti / `.debug` / taqiqlangan CEF bayrog'i tekshiruvi; `.sha256` sidecar + reliz manifesti), `scripts/build-installer-mac.sh` (pkgbuild + productbuild), `scripts/build-installer-win.mjs` (WiX v5 per-user MSI), `scripts/test-installer-artifacts.mjs`, `npm run test:plugin-installers`.
- **Per-user, imtiyozsiz:** mac `install-location=Library/Application Support/Adobe/CEP/extensions/com.frameflow` + `auth="none"` + `enable_currentUserHome` (administrator paroli SO'RALMAYDI); win `Scope="perUser"` → `%APPDATA%`. `/Library`, Program Files va boshqa Adobe holatiga tegilmaydi.
- **Fail-closed imzolash:** kredensiallar FAQAT env'dan (`FF_MAC_INSTALLER_IDENTITY`, `FF_NOTARY_*`, `FF_WIN_CERT`/`FF_WIN_CERT_SHA1` + `FF_WIN_CERT_PASS`, `FF_SIGNED_ZXP`). Yo'q yoki QISMAN bo'lsa build to'xtaydi; self-signed zaxira va standart parol YO'Q; imzo chegaralangan `_build.<plat>.XXXXXX` ichida, faqat hammasi muvaffaqiyatli tugagach atomik `mv`; nosozlikda na artefakt, na temp qoladi; parol/identika chop etilmaydi.
- **Ikki topilma (tuzatildi):** (1) `pkgbuild` har faylga AppleDouble `._` yozuvi qo'shardi (o'chirib bo'lmaydigan `com.apple.provenance` xatributi) — Payload/Bom stage papkasidan `cpio --format odc` + `mkbom` bilan qayta quriladi, endi payload flavor ro'yxatiga AYNAN teng. (2) `pkgbuild` doim `auth="root"` yozadi — PackageInfo patch bilan `auth="none"`.
- **CEP imzo konverti:** reliz payload'i `FF_SIGNED_ZXP` dagi `META-INF/**` + `mimetype` bilan keladi (aks holda AE imzosiz papkani yuklamaydi). `.zxp`ning qolgan tarkibi flavor ro'yxatiga mos bo'lmasa — rad etiladi. FAQAT imzosiz QA build'da `postinstall` shu foydalanuvchi uchun `PlayerDebugMode` yoqadi.
- **Testlar (hammasi PASS):** `test:plugin-installers` **160/160** — haqiqiy `pkgbuild`/`productbuild` bilan qurilgan `.pkg` ochib tekshiriladi (install-location, `auth`, domen, payload = flavor ro'yxati, AppleDouble yo'q); fail-closed 3 holat haqiqiy build bilan; "boshqa artefakt tegilmadi" SHA-256 bilan isbot; Windows toolchain PATH'dagi soxta `wix`/`signtool` bilan (build-zxp.sh testidagi naqsh) · `test:plugin-package` **47/47** · `test:plugin-updater` **118/118** · `test:release-contract` **108/108** · `test:plugin-download-state` **10/10**.
- **Kutilmoqda (EGA):** Apple Developer ID Installer sertifikati + notarizatsiya kaliti, Windows Authenticode (EV) sertifikati/token, Windows mashinasi yoki `windows-latest` CI (`dotnet tool install --global wix`). Keyin: imzolangan build → Admin → Releases → Publish → AE'da jonli E2E.
