# SESSION-REPORT — Windows migratsiyasi (1cc01ad auditi, 2026-07-22)

**1 commit, push/deploy/imzolash YO'Q. Diff FAQAT `plugins/after-effects-cep/scripts/installer-wix.mjs` + `test-installer-artifacts.mjs` + 3 hujjat. Backend/DB/auth/billing/landing/panel updater runtime/admin-mijoz UI/payload fayllari TEGILMAGAN.**

- **Topilgan bo'shliq:** mavjud foydalanuvchilarda MSI'dan OLDINGI (qo'lda nusxa yoki `.zxp`) o'rnatma bo'lishi mumkin; unda endi ICHKI hisoblangan `.debug`, `.debug.admin`, `AssetFlow_Admin.html`, `CSXS/manifest.admin.xml` qoladi. `MajorUpgrade` faqat MSI O'ZI o'rnatgan komponentni olib tashlaydi → Windows installeri bu qoldiqlarni qoldirib ketardi (macOS'da `postinstall` allaqachon tozalardi).
- **Tuzatish (`installer-wix.mjs`):** `obsoleteInstallFiles()` **yagona manba** sifatida import qilinadi; har bir yo'l uchun AYNAN bitta standart `<RemoveFile Directory="INSTALLFOLDER" [Subdirectory="CSXS"] Name="…" On="install"/>`. Qatorlar bitta per-user komponentda (`FF_LegacyCleanup`, HKCU keypath, `ComponentRef` bilan o'rnatiladi). Id — to'liq nisbiy yo'l hash'idan (deterministik, noyob).
- **Chegaralar (macOS `postinstall` siyosati bilan bir xil):** joker YO'Q · `RemoveFolder` YO'Q · CustomAction/skript YO'Q · papka o'chirilmaydi · `assetflow-data` va joriy payload TEGILMAYDI (ro'yxat payload bilan kesishsa generator fail-closed to'xtaydi). MSI `RemoveFiles` amali `InstallFiles` dan oldin ishlaydi — qoldiq yangi payload joylashishidan avval ketadi.
- **Testlar (hammasi PASS):** `test:plugin-installers` **229/229** (202 → 229; +27, shundan 26 migratsiya bloki) · `test:plugin-package` **47/47** · `test:plugin-updater` **118/118** · `test:release-contract` **108/108** · `test:plugin-download-state` **10/10** · `verify-public-copy` **67/67** · `npm run build -w apps/api` OK · `node --check` (4 mjs) + `bash -n` (mac skripti + generatsiya qilingan postinstall) + `xmllint --noout` (generatsiya qilingan `.wxs`) + `git show --check` toza.
- **Regressiya isboti:** 1cc01ad generatori bilan (test fayli o'zgarmagan holda) `test:plugin-installers` → **209 passed, 20 failed, exit 1**. Migratsiya eksportlari nom fazosi orqali olinadi, shuning uchun eski generatorda test halol "failed" beradi (import SyntaxError'i emas).
- **Qolgan cheklov (KOD EMAS):** kafolat generatsiya darajasida. **Haqiqiy imzolangan `.msi` hali qurilmagan** — `wix build` (WiX v4/v5 `Subdirectory` atributi), ICE validatsiyasi va eski o'rnatma ustidan haqiqiy upgrade Windows mashinasida / `windows-latest` CI'da tasdiqlanishi SHART. Apple Developer ID + notarizatsiya va Windows Authenticode (EV) kredensiallari ham hali yo'q.
