# SESSION-REPORT — Marketplace preflight: HAQIQIY kriptografik imzo tekshiruvi (2026-07-22)

**Kod tomoni TUGADI; Adobe'ga hech narsa topshirilmadi. Backend/DB/auth/pul-zona/deploy/installer/updater TEGILMAGAN.**

- **YANGI:** `plugins/after-effects-cep/scripts/marketplace-preflight.mjs` — fail-closed preflight. QA rejimi (kredensialsiz, `ZXPSignCmd` CHAQIRILMAYDI) jonli manbani + YANGI qurilgan mijoz arxivini tekshiradi; `--release` imzolangan `.zxp` va to'liq ega metadatani TALAB qiladi.
- **DIREKTOR AUDIT TUZATILDI (asosiy):** ilgari `--release` faqat `META-INF/signatures.xml` borligini + bayt tengligini ko'rib "HAQIQATAN imzolangan / RELIZ o'tdi" derdi — soxta konvertli oddiy zip o'tib ketardi. Endi Adobe **`ZXPSignCmd -verify <artefakt>`** argument massivi bilan (shell YO'Q) ishga tushadi, chiqish kodi AYNAN 0 shart; vosita yo'q / ishga tushmadi / kod ≠ 0 → fail-closed, muvaffaqiyat xabari CHIQMAYDI. Qidiruv joylari `build-zxp.sh` bilan bir xil, `--zxpsigncmd=` / `ZXPSIGNCMD_PATH` (maxfiy emas) bilan ANIQ beriladi. Vosita stdout/stderr'i hech qachon chop etilmaydi; `ZXP_CERT*` o'qilmaydi.
- **Halol nomlash:** "imzo konverti mavjud" · "konteyner tanasi JORIY mijoz payload baytlariga teng" — bular imzoni ISBOTLAMAYDI va endi shunday da'vo qilmaydi. Hujjatda: lokal preflight paketni kriptografik tasdiqlaydi, **yakuniy qabul/ishonch siyosati — Adobe portali / Creative Cloud**.
- **Metadata bo'shlig'i:** `owner.publisherAccountEmail` faqat "bo'sh emas" edi → endi konservativ email SHAKLI majburlanadi (`n/a` o'tmaydi); qiymat hech qayerga chop etilmaydi/uzatilmaydi.
- **Testlar:** `test:marketplace-preflight` **100/100** (78 → +22 = 14 ZXPSignCmd orkestratsiyasi + 5 skript o'z-tekshiruvi + 4 email − 1 ko'chirilgan integratsiya). Soxta tekshiruvchi fikstura FAQAT orkestratsiyani isbotlaydi (argv `["-verify", <artefakt>]`, exit 0/≠0, QA chaqirmasligi, chiqish sizmasligi) — kriptografik haqiqiylikni EMAS; testda shunday yozilgan. 8 mutatsiya probasi tekshiruvlar bo'sh emasligini tasdiqladi. Regressiya yo'q: plugin-package **47** · plugin-installers **244** · plugin-updater **118** · release-contract **108** · download-state **10** · ci-windows-installer **135**. `node --check`, paket verify (29 referens), `git diff --check` — toza.
- **Jonli tasdiq:** QA `67 passed, 0 failed` (exit 0). Haqiqiy `-- --release` → **exit 1**. Soxta `META-INF/signatures.xml` qo'shilgan zip imzolangan nom ostida real CLI orqali ham **O'TMADI** (vosita yo'q → fail-closed).
- **🔴 YAGONA KOD-YONDOSH TASHQI BLOKER:** bu mashinada `ZXPSignCmd` (PATH · Extension Manager · `~/bin` · `<repo>/tools` — hech qayerda) ham, imzolash identikasi ham yo'q → kriptografik tekshiruvni BAJARIB BO'LMAYDI, `-- --release` ataylab yiqiladi (zaiflashtirilmadi). Ega ishi: Adobe hisobi, sertifikat + `ZXPSignCmd`, listing matni, vizual assetlar, portalga topshirish.
