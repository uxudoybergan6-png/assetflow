# SESSION-REPORT — Task 3 auditi bo'yicha tuzatish (2026-07-22)

**1 commit, push/deploy YO'Q, hech qanday `.pkg`/`.msi` O'RNATILMADI, imzolash kredensiali ISHLATILMADI. Diff FAQAT `plugins/after-effects-cep/scripts/*` + hujjatlar; panel updater bloki, API/DB/admin/landing/to'lov/auth/storage TEGILMAGAN.**

- **Imzo endi BAYTGA bog'langan** (`installer-payload.mjs`): `.zxp` ichidagi konvertdan tashqari HAR BIR fayl allaqachon yig'ilgan lokal payload bilan bayt-ba-bayt + SHA-256 solishtiriladi. Avval faqat NOM ro'yxati tekshirilardi — nom bir xil, bayt boshqa arxiv o'tib ketardi (eski kodda isbotlandi).
- **Arxiv `unzip -d` bilan yoyilmaydi:** ZIP markaziy katalogi xom, TARTIBLANGAN ro'yxat sifatida o'qiladi (Set YO'Q). Rad sabablari: takrorlangan nom · `..`/absolyut/buzuq (ASCII bo'lmagan) yo'l · **symlink/qurilma yozuvi** · shifrlash · noma'lum siqish · CRC-32/o'lcham nomuvofiqligi. Konvert yo'llari YOPIQ ro'yxat: `META-INF/signatures.xml` (majburiy) + `mimetype`; faqat ular oddiy fayl sifatida yoziladi.
- **Windows:** chiqish haqiqatan MSI ekani (OLE `d0 cf 11 e0 a1 b1 1a e1` + minimal hajm) `wix build`dan KEYIN ham, `signtool`dan KEYIN ham tekshiriladi — ixtiyoriy bo'sh bo'lmagan baytlar rad. WiX Directory Id endi TO'LIQ yo'ldan (`css/fonts` ↔ `js/fonts` to'qnashuvi yo'q); generator alohida `installer-wix.mjs` (testda to'g'ridan sinaladi).
- **macOS (buzg'unchi xulq olib tashlandi):** `preinstall` BUTUNLAY yo'q — ishlayotgan plagin yangi payload joylashishidan oldin hech qachon o'chirilmaydi. `postinstall` generatsiya qilinadi va faqat o'rnatma muvaffaqiyatli tugagach ANIQ nomli eski fayllarni (ichki Admin sirti + `.debug*`) olib tashlaydi: `find`/`rm -rf`/joker YO'Q, `assetflow-data` tilga ham olinmaydi. **PlayerDebugMode / `defaults write` ga har qanday yozuv olib tashlandi** — imzosiz QA paketi uchun dasturchi rejimini operator o'zi qo'lda yoqadi (hujjatda halol yozildi).
- **Testlar (hammasi PASS):** `test:plugin-installers` **202/202** (160 → 202; yangi: bayt-bog'lash, buzg'unchi arxiv fiksturalari, MSI tuzilma, WiX Id to'qnashuvi, manba skaneri) · `test:plugin-package` **47/47** · `test:plugin-updater` **118/118** · `test:release-contract` **108/108** · `test:plugin-download-state` **10/10** · `verify-public-copy` **67/67**. Yangi testlar b8f94f9 kodida yiqilishi alohida isbotlandi (mutatsiya qilingan `.zxp` va symlink yozuvi eski kodda QABUL qilinardi).
- **Kutilmoqda (EGA):** Apple Developer ID Installer + notarizatsiya kaliti, Windows Authenticode (EV), Windows/`windows-latest` CI (`dotnet tool install --global wix`) — imzolangan build shundan keyin.
