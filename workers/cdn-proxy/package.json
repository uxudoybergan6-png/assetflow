{
  "name": "frameflow-cdn-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "FrameFlow CDN proxy Worker (cdn.getframeflow.app) — serves display assets from the closed GCS bucket via the isPublicReadKey allow-list",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.90.0"
  }
}
